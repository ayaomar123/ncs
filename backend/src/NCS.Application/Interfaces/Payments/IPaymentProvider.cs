namespace NCS.Application.Interfaces.Payments;

public interface IPaymentProvider
{
    // Phase 2: integrate a real payment provider (Stripe/PayPal/etc.)
    // In MVP this remains unused; donation requests are stored as intent only.
}
